---
description: Use vibecoding.skill in Cursor as a native project rule to distill AI collaboration habits, generate share cards, export bundles, and continue in a distilled style.
alwaysApply: false
---

# vibecoding.skill

读取真实的 Code Agent 记录，优先给出：

- 当前等级和阶段
- 协作画像
- 最稳的地方
- 最该补的地方

用户继续追问时，再补：

- 分享卡
- 完整报告
- 共享包
- 目标等级训练建议

## Workflow

1. 先判断输入是单次会话、时间窗聚合，还是导出包。
2. 自动读取可用轨迹。
3. 轨迹过长时，优先保留用户 prompt 原文，压缩 AI 回复，再按用户回合分块，先做局部判断，再汇总总报告。
4. 先输出人话版结论：当前等级、协作画像、强项、短板。
5. 用户要分享卡时，再生成卡片。
6. 用户要共享时，再导出完整共享包。
7. 用户要升级时，再给训练方案。

## Result Skill Handoff

当用户给出导出包并要求按其中风格继续协作时：

1. 先读取 `PROFILE.md`、`REPORT.md`、`SKILL.md`。
2. 识别其中的结果 skill。
3. 后续协作默认按这个结果 skill 的行为层执行。

## Language Rules

- 默认先说人话。
- 用户提到“修仙”或“境界”时，卡片和相关文案切到修仙风格。
- 文案要短，能直接复制给别人看。

